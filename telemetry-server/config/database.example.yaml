# 复制为 database.yaml 后按内网环境修改；生产密码不得提交到版本库。
host: 127.0.0.1
port: 3306
database: aaw_telemetry
username: aaw
password: change-me
charset: utf8mb4
# 以下连接池上限按应用进程计算；总连接预算约为进程数 × (pool_size + max_overflow)。
pool_size: 5
max_overflow: 10
pool_timeout_seconds: 30
