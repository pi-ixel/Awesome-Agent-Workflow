# AR07758-支持快速退款接口相关功能-支付审计模块测试用例设计

> ⚠ 本文件是**测试 fixture**，由 module-test-design 阶段产出（模拟）。它本身大体合法，但**故意只覆盖主路径，漏掉关键失败路径和风险**，配合说明书的可测试性输入不足，用于验证 gate 的"验证闭环性"维度能否抓出来。

## 1. 测试范围与策略

| 项目 | 内容 |
|---|---|
| 目标模块 | 支付审计模块 |
| 输入正式说明书 | 模块详细设计说明书.md |
| 输入 context | .context/详细设计上下文.md |
| 测试设计状态 | 完成 |
| 策略摘要 | 采用最小充分验证集，优先覆盖 P0/P1 设计风险，允许一条用例覆盖多个设计目标。 |
| 阻塞或待补设计摘要 | 无 |

## 2. 最小充分测试用例集

> 排版：`### 2.x` 测试层级 → `#### 2.x.y` 功能点/场景 → `##### TCn` 用例条目。只列本次设计能支撑的层级。

### 2.1 单元测试

#### 2.1.1 退款事件写入与查询 - 主路径

##### TC1 正常退款事件写入

- 优先级：P0
- 覆盖目标：R1 主路径
- 场景：正常退款事件写入
- 建议位置：src/payment-audit/test/AuditRecorderTest.java
- 自动化：JUnit5
- 前置：mock audit_log
- 输入：发送退款事件
- 预期：写入成功
- 断言：audit_log 含记录

##### TC2 按 refundId 查询审计记录

- 优先级：P0
- 覆盖目标：R2 主路径
- 场景：按 refundId 查询
- 建议位置：src/payment-audit/test/AuditQueryServiceTest.java
- 自动化：JUnit5
- 前置：mock 数据
- 输入：查询 refundId
- 预期：返回记录
- 断言：返回非空列表

### 2.2 契约测试

#### 2.2.1 审计查询接口契约

##### TC3 GET 接口返回结构符合契约

- 优先级：P1
- 覆盖目标：K1
- 场景：GET 接口返回结构
- 建议位置：src/payment-audit/test/AuditControllerTest.java
- 自动化：JUnit5
- 前置：无
- 输入：GET 请求
- 预期：返回 200
- 断言：返回结构符合契约

> ⚠ 配合说明书植入的缺陷，本测试设计**故意遗漏**：
> - 退款写入失败的失败路径用例
> - 订单更新失败的回滚用例（D3 相关）
> - 退款幂等性用例（A3 相关，说明书把它当事实了，这里也无对应验证）
> - 契约 K2（RefundAuditEvent DTO）字段一致性用例
>
> gate 的"验证闭环性"检查应判定这些 P0/P1 目标未被覆盖，给出 GAP 回流建议。

## 3. 覆盖矩阵

| TOBE 决策 / 契约 / 风险 | 优先级 | 覆盖用例 | 覆盖状态 | 未覆盖原因 |
|---|---|---|---|---|
| D1 主路径 | P0 | TC1 | 已覆盖 | |
| D2 查询主路径 | P0 | TC2 | 已覆盖 | |
| K1 接口契约 | P1 | TC3 | 已覆盖 | |
| D3 订单更新 | P0 | （无） | 未覆盖 | 缺失败路径和回滚用例 |
| 退款幂等 | P0 | （无） | 未覆盖 | 说明书未明确，无法设计用例 |
| K2 DTO 契约 | P1 | （无） | 未覆盖 | 字段在说明书中不一致，无法断言 |

## 4. 缺口与回流建议

> ⚠ 本节**故意留空**。按照 module-test-design SKILL.md，未覆盖的 P0/P1 目标必须写入缺口表并给出回流阶段（回 TOBE / 回 ASIS / 上游确认）。这里留空会让 gate 发现：测试设计未把已知缺口显性化。

| 缺口编号 | 缺口类型 | 关联 TOBE 决策 / 契约 / 风险 | 缺口描述 | 影响 | 回流阶段 | 补齐后通过条件 |
|---|---|---|---|---|---|---|

## 5. 变更记录

| 轮次 | 变更摘要 | 来源 |
|---|---|---|
| 第 1 轮 | 初始生成 | fixture |
