name: ar-split
execution: prompt
prompt:
  template: "prompts/ar-split.md"
input:
  - path: ".sdd/{SR}/SR-design.md"
    required: true
output: []
data_prompt:
  description: "根据用户选择构造 --data：拆分时使用 ars；不拆分时使用 mode=no_split。"
