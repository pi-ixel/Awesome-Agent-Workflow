# T[N]：[任务标题]

## 任务元信息
- 任务编号：T[N]
- 前置任务：[无 / T1, T2 等，多个用逗号分隔；必填]
- 执行顺序：必须在所有前置任务完成后执行（多个前置任务都完成才能开始）
- 代码影响模块：[模块名称]
- 交付文件：[具体文件路径列表]

## 详细设计

### 背景与目标
[从详细设计文档中提取：为什么要做这个任务，解决什么问题，达成什么目标]

### 代码影响

| 模块名称 | 修改文件 | 修改内容 |
|---------|---------|---------|
| [模块名] | [文件路径] | [修改内容描述] |

### 流程图

[从详细设计文档中原样复制本任务相关的流程图（Mermaid）；若设计文档有多张图，复制涉及本任务模块的图；不得自行重画或简化]

```mermaid
[流程图内容]
```

### 实现方案
[从详细设计文档中提取该任务相关的实现方案：核心逻辑、关键数据结构、算法、流程]

#### 接口设计
[从详细设计文档中提取该任务涉及的接口设计]

**接口原型**
- [接口/函数签名]

**返回值语义**
- [返回值说明]

**错误码**
- [错误码及含义]

#### 错误处理
[从详细设计文档中提取该任务的错误处理策略]

### 非功能约束
[从详细设计文档中提取该任务相关的非功能约束：性能目标、安全要求、可观测性要求]

## 用例设计

[从详细设计文档的"验证和测试"章节提取并转换为结构化用例；至少包含 3-5 个用例，覆盖正常流程、异常流程、边界条件]

### 用例 TC-T[N]-01：[用例标题]

**前置条件**
- 系统状态：[描述系统需要处于什么状态]
- 数据准备：[需要准备的测试数据]
- 环境配置：[相关配置项和值]

**测试步骤**
1. [操作描述]，参数 `[具体参数]`
2. [操作描述]
3. [操作描述]

**预期结果**
- 步骤 1：[具体预期结果，包含返回值、状态码、数据变化]
- 步骤 2：[具体预期结果]
- 步骤 3：[具体预期结果]
- 最终状态：[系统最终应处于什么状态]

**后置条件**
- 清理：[需要清理的测试数据或状态]
- 状态恢复：[需要恢复的系统状态]

**依赖任务**（可选）：[T[X] — 该用例需在 T[X] 完成后回归验证；不填表示无下游依赖]

### 用例 TC-T[N]-02：[用例标题]

**前置条件**
- [...]

**测试步骤**
1. [...]

**预期结果**
- [...]

**后置条件**
- [...]

**依赖任务**（可选）：[T[X] — 该用例需在 T[X] 完成后回归验证；不填表示无下游依赖]

### 用例 TC-T[N]-03：[用例标题]

**前置条件**
- [...]

**测试步骤**
1. [...]

**预期结果**
- [...]

**后置条件**
- [...]

**依赖任务**（可选）：[T[X] — 该用例需在 T[X] 完成后回归验证；不填表示无下游依赖]

## DoD Checklist

- [ ] **功能**（一个 task 可含多个功能点，逐条列出，每条单独勾选）：
  - [ ] [功能点 1：具体验收标准]
  - [ ] [功能点 2：具体验收标准]
  - [ ] [功能点 3：具体验收标准]
- [ ] **测试**：
  - [ ] **编写用例**：为所有非挂账用例（未标 `依赖任务` 的 TC-T[N]-01 到 TC-T[N]-0X）编写自动化测试代码（项目无测试框架或用例不适合自动化时，改为按测试步骤列出手动验证清单）
  - [ ] **运行用例**：所有非挂账用例的测试代码运行通过（手动用例按清单逐条执行通过）；挂账用例已登记到 overview 挂账用例登记表
- [ ] **文档**：[如涉及 API 变更或配置变更，更新相关文档]
- [ ] **可观测性**：[如涉及关键路径，添加必要的日志/指标/追踪]

## 总结

本任务完成后，应达成以下目标：
- [目标 1]
- [目标 2]
- [目标 3]

下一步：执行下一个可开工任务（前置任务都已完成的 Pending 任务中编号最小者；如无则全部完成）
